# Riesgos y preguntas abiertas

- `develop` aún no existe en ninguno de los dos repositorios.
- No existe contrato REST aprobado.
- El frontend todavía está vacío; React/Angular no puede inferirse.
- La retención concurrente de slots y su expiración no están especificadas.
- Faltan decisiones de zona horaria, errores, CORS y ciclo de vida detallado de JWT.
- `database/reference/` está presente, pero su uso debe autorizarlo el trainer.
- La raíz no tiene Git; el versionado del `AGENTS.md` raíz queda pendiente de decisión.
- `citas-api/.env.example` tenía una modificación local previa y debe preservarse.
